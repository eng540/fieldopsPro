# FieldOps V4 Backend

Modular FastAPI backend for FieldOps V4.

This README exists as the package metadata entry point used by Poetry; runtime documentation is maintained under the repository `docs/` tree.
